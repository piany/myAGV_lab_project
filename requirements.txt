# myagv_lab Python dependencies
# Install: pip3 install -r requirements.txt

numpy>=1.24
imageio>=2.31
pyperplan>=1.3
anthropic>=0.20
openai>=1.0
matplotlib>=3.7

# Testing
pytest>=7.0
pytest-timeout>=2.1

# Optional: for ROS2 real-robot mode, install separately:
#   sudo apt install ros-humble-navigation2 ros-humble-nav2-bringup
#   sudo apt install ros-humble-slam-toolbox
